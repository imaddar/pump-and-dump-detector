this is where the data collection itself happens. We organize the scripts for collecting data here, both for the offline data collection, as well as the streaming data. This keeps all of the data collection logic in one place, while separating it from the datasets themselves, as the files are thematically different in nature.

## TO-DO Later
combine the csv files together into one dataset